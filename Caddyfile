:80 {
    # API backend
    handle /api/* {
        reverse_proxy backend:3000
    }

    # Fichiers uploadés (logos publics + fiches techniques protégées par le backend)
    handle /uploads/* {
        reverse_proxy backend:3000
    }

    # Frontend React (tout le reste)
    handle {
        reverse_proxy frontend:80
    }
}
